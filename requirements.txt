# Core runtime (always required). Python 3.11+.
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
numpy>=1.26
pygltflib>=1.16
scipy>=1.11
pydantic>=2.6
pyyaml>=6.0
httpx>=0.27
aiohttp>=3.9
soundfile>=0.12
psutil>=5.9
rich>=13.7
python-dotenv>=1.0

# Conversation voice input (mic -> text). CPU int8 whisper; optional at runtime,
# the /stt endpoint returns 503 with guidance when absent.
faster-whisper>=1.0

# Optional heavy / hardware-specific runtimes. The system lazy-imports these and
# degrades to deterministic CPU paths when they are absent, so install only what
# your machine supports. Uncomment or `pip install '.[ml]'` to enable.
# torch>=2.2
# torchaudio>=2.2
# onnxruntime>=1.17
# opencv-python>=4.9
# mediapipe>=0.10
# librosa>=0.10
# av>=11.0

# Optional voice backends (install at least ElevenLabs SDK for primary engine):
# elevenlabs>=1.0
# piper-tts>=1.2
# Fish Speech S2 is installed via scripts/ (see install.sh) because it is a
# source checkout, not a stable PyPI wheel.

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
mypy>=1.9
